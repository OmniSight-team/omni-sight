# TODO Phase B
# Tier 1 — Concept-Vector Trigger
# Cheap cosine-similarity gate against a set of concept vectors.
# Admits a candidate only if similarity exceeds threshold θ₁.
# Suppresses obvious non-events before any model inference runs.
