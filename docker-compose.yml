services:
  rabbitmq:
    image: rabbitmq:3.13-management
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    configs:
      - source: rabbitmq_conf
        target: /etc/rabbitmq/rabbitmq.conf
      - source: rabbitmq_definitions
        target: /etc/rabbitmq/definitions.json
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  qdrant:
    image: qdrant/qdrant:v1.11.0
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - .docker-volumes/qdrant:/qdrant/storage
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6333/readyz"]
      interval: 10s
      timeout: 5s
      retries: 5

  # --- Phase A services (uncomment when Dockerfiles are ready) ---
  # ingestion:
  #   build:
  #     context: .
  #     dockerfile: services/ingestion/Dockerfile
  #   depends_on:
  #     rabbitmq:
  #       condition: service_healthy
  #     qdrant:
  #       condition: service_healthy
  #   environment:
  #     RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672/
  #     QDRANT_URL: http://qdrant:6333
  #     LOG_LEVEL: INFO

  # ai_worker_frame:
  #   build:
  #     context: .
  #     dockerfile: services/ai_worker_frame/Dockerfile
  #   depends_on:
  #     rabbitmq:
  #       condition: service_healthy
  #     qdrant:
  #       condition: service_healthy
  #   environment:
  #     RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672/
  #     QDRANT_URL: http://qdrant:6333
  #     LOG_LEVEL: INFO

  # ai_worker_temporal:
  #   build:
  #     context: .
  #     dockerfile: services/ai_worker_temporal/Dockerfile
  #   depends_on:
  #     rabbitmq:
  #       condition: service_healthy
  #     qdrant:
  #       condition: service_healthy
  #   environment:
  #     RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672/
  #     QDRANT_URL: http://qdrant:6333
  #     LOG_LEVEL: INFO

  # query_service:
  #   build:
  #     context: .
  #     dockerfile: services/query_service/Dockerfile
  #   ports:
  #     - "8000:8000"
  #   depends_on:
  #     rabbitmq:
  #       condition: service_healthy
  #     qdrant:
  #       condition: service_healthy
  #   environment:
  #     RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672/
  #     QDRANT_URL: http://qdrant:6333
  #     LOG_LEVEL: INFO

  # dashboard:
  #   build:
  #     context: services/dashboard
  #     dockerfile: Dockerfile
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - query_service

  # --- Phase B services (uncomment next semester) ---
  # pfm:
  #   build:
  #     context: .
  #     dockerfile: services/pfm/Dockerfile

  # verification:
  #   build:
  #     context: .
  #     dockerfile: services/verification/Dockerfile

  # rag_forensics:
  #   build:
  #     context: .
  #     dockerfile: services/rag_forensics/Dockerfile

configs:
  rabbitmq_conf:
    file: ./infra/docker/rabbitmq/rabbitmq.conf
  rabbitmq_definitions:
    file: ./infra/docker/rabbitmq/definitions.json
